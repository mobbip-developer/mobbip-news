import { formatContent, removeUnwantedContent } from './formatContent';
import { getMetaDescriptionFromContent } from './getMetaDescriptionFromContent';

export const contentUtils = {
  getMetaDescriptionFromContent,
  removeUnwantedContent,
  formatContent,
};
